name: Legacy GitHub Pages Deployment Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm deployment hold
        run: |
          echo "GitHub Pages deployment is disabled."
          echo "www.viliganscommandcorp.com remains the active pre-launch site."
          echo "The apex is reserved for a future Doc D AI deployment."
          echo "This workflow does not build, publish, deploy, or change DNS."
